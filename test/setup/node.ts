/**
 * Setup for the **data** project: plain Node, real `better-sqlite3`, no React Native runtime.
 * Deliberately thin — this is the suite that has to stay in the milliseconds.
 */
import './common';
