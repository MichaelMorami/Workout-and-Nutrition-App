import { Text, View } from 'react-native';

// Placeholder screen — deliberately unstyled. Real design lands once
// design-lead's visual design is approved.
export default function TodayScreen() {
  return (
    <View>
      <Text>Today</Text>
    </View>
  );
}
